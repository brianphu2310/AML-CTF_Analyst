# Deployment note: these floors are the oldest versions the app is tested against
# (Streamlit Cloud installs the newest that its Python version allows).
streamlit>=1.49,<2
plotly>=5.24,<8
pandas>=2.2,<4
numpy>=1.26,<3
python-docx>=1.1,<2
